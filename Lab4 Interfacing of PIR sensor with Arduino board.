void setup()
{
 pinMode(12, INPUT);
 pinMode(4,OUTPUT);
 pinMode(1,OUTPUT);
}
void loop()
{
 if (digitalRead(12) ==1){
   digitalWrite(1,HIGH);
   digitalWrite(4,HIGH);
 }
 else{
   digitalWrite(1,LOW);
   digitalWrite(4,LOW);
 }
}
